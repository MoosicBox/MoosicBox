# Workspace-level clippier configuration
# This defines common toolchains and dependencies for workspace-level CI setup

# Common toolchains needed for full workspace CI
[[dependencies]]
toolchain = "cargo-machete"

[[dependencies]]
toolchain = "taplo"
