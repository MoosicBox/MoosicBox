[package]
authors     = ["Braden Steffaniak"]
description = "Simulated cancel async runtime usage example"
edition     = "2024"
license     = "MIT"
name        = "async_cancel"
publish     = false
repository  = "https://github.com/MoosicBox/MoosicBox"
version     = "0.1.0"

[dependencies]
gimbal_async = { workspace = true, features = ["simulator", "time", "util"] }

ctrlc             = { workspace = true }
pretty_env_logger = { workspace = true }

[features]
default = []

fail-on-warnings = []
