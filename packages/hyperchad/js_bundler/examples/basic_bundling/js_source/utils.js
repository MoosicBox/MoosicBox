// Utility functions for the example application

export function greet(name) {
    return `Hello from ${name} bundler!`;
}

export function capitalize(str) {
    return str.charAt(0).toUpperCase() + str.slice(1);
}
