// Math utilities for the example application

export function calculate(a, b) {
    return add(a, b) * multiply(2, 1);
}

function add(x, y) {
    return x + y;
}

function multiply(x, y) {
    return x * y;
}
