# hyperchad_shared_state_bridge

HyperChad integration bridge for `hyperchad_shared_state`.

This crate converts HyperChad route/action context into shared-state envelopes and
keeps the shared-state runtime independent from renderer and routing internals.
