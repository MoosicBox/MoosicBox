import { onAttr } from './core';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
onAttr('v-onclickoutside', ({ element, attr }) => {
    // element.onclickoutside = () => {
    //     handleError('onclickoutside', () => eval(attr));
    // };
});
