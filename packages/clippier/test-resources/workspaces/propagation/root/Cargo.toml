[package]
edition.workspace = true
name              = "root"
version.workspace = true

[features]
fail-on-warnings = []
nightly          = []

[dev-dependencies]
middle = { path = "../middle" }

[dependencies]
leaf = { path = "../leaf" }
