[package]
edition = "2021"
name    = "shared-utils"
version = "0.1.0"

[dependencies]
serde = { workspace = true }

[features]
default          = ["json"]
fail-on-warnings = []
json             = ["serde/derive"]
logging          = []
