[package]
edition = "2021"
name    = "api"
version = "0.1.0"

[dependencies]
models  = { path = "../models" }
reqwest = { workspace = true }
serde   = { workspace = true }
tokio   = { workspace = true }

[features]
fail-on-warnings = []
