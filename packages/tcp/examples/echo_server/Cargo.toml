[package]
description = "Example demonstrating basic TCP server/client usage in switchy_tcp"
edition     = { workspace = true }
keywords    = ["client", "example", "server", "switchy_tcp", "tcp"]
license     = { workspace = true }
name        = "switchy_tcp_echo_server_example"
publish     = false
repository  = { workspace = true }
version     = "0.1.0"

[[bin]]
name = "echo_server_example"
path = "src/main.rs"

[dependencies]
switchy_tcp = { workspace = true, default-features = false, features = [
    "tokio",
] }
tokio = { workspace = true, features = [
    "io-util",
    "macros",
    "net",
    "rt-multi-thread",
    "time",
] }

[features]
default          = []
fail-on-warnings = ["switchy_tcp/fail-on-warnings"]
