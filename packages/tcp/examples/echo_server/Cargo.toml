[package]
name = "switchy_tcp_echo_server_example"
version = "0.1.0"
edition = { workspace = true }
publish = false
description = "Example demonstrating basic TCP server/client usage in switchy_tcp"
license = { workspace = true }
repository = { workspace = true }
keywords = ["example", "switchy_tcp", "tcp", "server", "client"]

[[bin]]
name = "echo_server_example"
path = "src/main.rs"

[dependencies]
switchy_tcp = { workspace = true, default-features = false, features = ["tokio"] }
tokio = { workspace = true, features = ["io-util", "macros", "net", "rt-multi-thread", "time"] }

[features]
default = []
fail-on-warnings = ["switchy_tcp/fail-on-warnings"]
