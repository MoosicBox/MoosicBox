# moosicbox_log_runtime

Generic runtime logging utilities:

- OS-aware state/log directory resolution with env overrides
- directory creation helpers
- optional tracing initialization with rolling file sinks
