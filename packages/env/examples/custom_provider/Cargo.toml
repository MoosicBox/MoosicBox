[package]
name = "switchy_env_custom_provider_example"
version = "0.1.0"
edition = { workspace = true }
publish = false
description = "Example demonstrating custom EnvProvider implementations in switchy_env"
license = { workspace = true }
repository = { workspace = true }
keywords = ["example", "switchy_env", "provider", "custom"]

[[bin]]
name = "custom_provider_example"
path = "src/main.rs"

[dependencies]
switchy_env = { workspace = true }

[features]
default = []
fail-on-warnings = ["switchy_env/fail-on-warnings"]
