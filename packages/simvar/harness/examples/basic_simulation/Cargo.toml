[package]
name = "simvar_harness_basic_simulation_example"
version = "0.1.0"
edition = { workspace = true }
publish = false
description = "Example demonstrating basic simulation usage in simvar_harness"
license = { workspace = true }
repository = { workspace = true }
keywords = ["example", "simvar_harness", "simulation", "testing"]

[[bin]]
name = "basic_simulation_example"
path = "src/main.rs"

[dependencies]
simvar_harness = { workspace = true }

log = { workspace = true }

[features]
default = []
fail-on-warnings = ["simvar_harness/fail-on-warnings"]
